import { Canvas, Meta } from "@storybook/blocks";
import Example from "./example.tsx";
import * as IsraelRD from "./israelrd.stories.tsx";

<Meta of={IsraelRD} title="IsraelRD" />

# IsraelRD
Componente Switch que cambia el color de fondo de la página.

#### Example - Default
<Canvas of={IsraelRD.Default} />

## Usage
```ts
import { IsraelRD } from 'milibreria';

function Example() {
  const [checked, setChecked] = React.useState(true);

  const handleChange = (event: React.ChangeEvent<HTMLInputElement>) => {
    setChecked(event.target.checked);
    document.body.style.backgroundColor = event.target.checked ? '#333' : '#fff';
  };

  return (
    <IsraelRD
      checked={checked}
      onChange={handleChange}
    />
  );
}

export default Example;

#### Example - Alternate
<Canvas of={IsraelRD.Alternate} />

## Usage
```ts

import { IsraelRD } from 'milibreria';

function Example() {
  const [checked, setChecked] = React.useState(false);

  const handleChange = (event: React.ChangeEvent<HTMLInputElement>) => {
    setChecked(event.target.checked);
    document.body.style.backgroundColor = event.target.checked ? '#333' : '#fff';
  };

  return (
    <IsraelRD
      checked={checked}
      onChange={handleChange}
    />
  );
}

export default Example;